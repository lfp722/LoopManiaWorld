package unsw.loopmania.enemy;

public class Slug extends Enemy {

    public Slug(int supportRange, int detectRange, Location position, double critRate, int lv) {
        super(supportRange, detectRange, position, critRate, lv);
    }
    
}
